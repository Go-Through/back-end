const { callService, baseParams, models } = require('./init-module');
const { getTest } = require('./manage-test');

async function getMyPlace(userIdx, pos, order, numOfRow) {
}

async function getTripInfo(contentId){
}

module.exports = {
  getMyPlace,
  getTripInfo,
};
